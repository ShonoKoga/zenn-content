"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.mdContainerMessage = exports.mdContainerDetails = void 0;
var md_base_1 = require("./md-base");
// containers
// ref: https://github.com/markdown-it/markdown-it-container
// ::: details Detail
//   summary comes here
// :::
exports.mdContainerDetails = {
    validate: function (params) {
        return params.trim().match(/^details\s+(.*)$/);
    },
    render: function (tokens, idx) {
        var m = tokens[idx].info.trim().match(/^details\s+(.*)$/);
        if (tokens[idx].nesting === 1) {
            // opening tag
            return ("<details><summary>" +
                md_base_1.escapeHtml(m[1]) +
                '</summary><div class="details-content">');
        }
        else {
            // closing tag
            return "</div></details>\n";
        }
    },
};
// ::: message alert
//   text
// :::
exports.mdContainerMessage = {
    validate: function (params) {
        return params.trim().match(/^message\s*(.*)$/);
    },
    render: function (tokens, idx) {
        var m = tokens[idx].info.trim().match(/^message\s*(.*)$/);
        if (tokens[idx].nesting === 1) {
            // opening tag
            return '<div class="msg ' + md_base_1.escapeHtml(m[1]) + '">';
        }
        else {
            // closing tag
            return "</div>\n";
        }
    },
};
